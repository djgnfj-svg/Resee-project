# Resee 환경변수 템플릿
# 이 파일을 복사하여 .env (개발용) 또는 .env.prod (프로덕션용)로 사용하세요

# Django Core Settings
SECRET_KEY=django-insecure-development-key-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1

# Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=resee_dev
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/resee_dev

# PostgreSQL Database (개발/프로덕션 공통)
# 프로덕션에서는 .env.prod에서 별도 설정

# Frontend Settings
FRONTEND_URL=http://localhost:3000
REACT_APP_API_URL=http://localhost:8000/api
REACT_APP_GOOGLE_CLIENT_ID=

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@localhost
COMPANY_NAME=Resee
SUPPORT_EMAIL=support@localhost
ENFORCE_EMAIL_VERIFICATION=False

# Google OAuth (선택사항)
GOOGLE_OAUTH2_CLIENT_ID=
GOOGLE_OAUTH2_CLIENT_SECRET=

# Django Settings Module
DJANGO_SETTINGS_MODULE=resee.settings.development

# Timezone
TIME_ZONE=Asia/Seoul

# AI Services
ANTHROPIC_API_KEY=