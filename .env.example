# ================================
# Resee v0.1.0 환경 설정 템플릿
# ================================
# 이 파일을 .env로 복사한 후 실제 값으로 수정하세요.
# deploy-auto.sh 스크립트를 사용하면 자동으로 생성됩니다.

# 🎯 필수 설정 (반드시 변경)
SECRET_KEY=your-secret-key-here-make-it-long-and-random-50-characters
ANTHROPIC_API_KEY=your-anthropic-api-key-from-anthropic-console

# 🌐 서버 설정  
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain-or-ip
ENVIRONMENT=production

# 📊 데이터베이스 (Docker Compose에서 자동 설정)
DATABASE_URL=postgresql://resee_user:resee_password@db:5432/resee_db
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=amqp://resee:resee_password@rabbitmq:5672//
CELERY_RESULT_BACKEND=redis://redis:6379/0

# 📧 이메일 설정
ENFORCE_EMAIL_VERIFICATION=False
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# 프로덕션에서는 실제 이메일 서비스 사용:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# 🔑 Google OAuth (선택사항 - 소셜 로그인 사용 시)
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH2_CLIENT_SECRET=your-google-client-secret

# 🖥️ 프론트엔드 설정
REACT_APP_API_URL=http://your-domain-or-ip/api
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id

# 🔒 보안 설정
RATE_LIMIT_ENABLE=True
CSRF_TRUSTED_ORIGINS=http://your-domain-or-ip,https://your-domain-or-ip
CORS_ALLOWED_ORIGINS=http://your-domain-or-ip,https://your-domain-or-ip

# 📈 모니터링 및 로깅
SENTRY_DSN=
LOG_LEVEL=INFO