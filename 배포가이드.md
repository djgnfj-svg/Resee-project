# 🚀 Resee AWS 배포 가이드

> **AWS 환경에 최적화된 배포 가이드** ⏱️ 총 60분 소요

---

## 🎯 AWS 준비물 (15분)

### AWS 서비스 설정
- [ ] **EC2**: Ubuntu 22.04 LTS 인스턴스 생성
- [ ] **ALB**: Application Load Balancer (SSL 터미네이션용)
- [ ] **Route 53**: 도메인 DNS 관리 (선택사항)
- [ ] **RDS**: PostgreSQL 인스턴스 (선택사항, Docker도 가능)
- [ ] **ElastiCache**: Redis 클러스터 (선택사항, Docker도 가능)

### 외부 서비스
- [ ] **도메인**: 원하는 도메인 구매
- [ ] **Google OAuth**: [Google Console](https://console.cloud.google.com/)에서 앱 등록
- [ ] **AWS SES**: 이메일 발송용 (또는 Gmail SMTP)

---

## 🔧 1단계: EC2 인스턴스 설정 (20분)

### EC2 인스턴스 생성
```bash
# AMI: Ubuntu 22.04 LTS
# 인스턴스 유형: t3.medium 이상 권장
# 보안 그룹: HTTP(80), HTTPS(443), SSH(22) 허용
```

### SSH 접속 후 환경 설정
```bash
# 시스템 업데이트
sudo apt update && sudo apt upgrade -y

# Docker 설치
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh
sudo usermod -aG docker ubuntu

# Docker Compose 설치
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 필수 패키지 설치
sudo apt install -y git htop tree

# 디렉토리 생성
sudo mkdir -p /opt/Resee /var/log/resee /backups
sudo chown -R ubuntu:ubuntu /opt/Resee /var/log/resee /backups

echo "✅ EC2 설정 완료! 터미널을 다시 시작하세요."
```

**🔄 터미널 재시작 또는 `newgrp docker` 실행**

---

## 📥 2단계: 소스코드 다운로드 (5분)

```bash
# 소스코드 다운로드
cd /opt
sudo git clone https://github.com/your-username/Resee.git  # 본인 저장소 주소로 변경
sudo chown -R ubuntu:ubuntu Resee
cd Resee
```

---

## ⚙️ 3단계: 환경설정 (15분) ⚠️ 가장 중요!

### 환경변수 파일 생성
```bash
# 환경변수 파일 복사 및 수정
cp .env.production.example .env.production
nano .env.production
```

### 📝 .env.production 파일에서 AWS용으로 수정:

```env
# 🔑 보안 키 (반드시 변경!)
SECRET_KEY=django-insecure-바꿔주세요-50자-이상-랜덤-문자열
POSTGRES_PASSWORD=데이터베이스-강한-비밀번호
RABBITMQ_PASSWORD=큐-강한-비밀번호

# 🌐 도메인 설정 (ALB 도메인 또는 본인 도메인)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,your-alb-xxx.us-east-1.elb.amazonaws.com
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
REACT_APP_API_URL=https://yourdomain.com

# 🔒 SSL/HTTPS 설정 제거됨 (AWS ALB에서 처리)
# AWS ALB/CloudFront에서 SSL 터미네이션하므로 앱에서는 HTTP 사용

# 🔐 Google OAuth (Google Console에서 발급받은 것)
GOOGLE_OAUTH2_CLIENT_ID=본인-구글-클라이언트-ID
GOOGLE_OAUTH2_CLIENT_SECRET=본인-구글-클라이언트-시크릿
REACT_APP_GOOGLE_CLIENT_ID=본인-구글-클라이언트-ID

# 📧 이메일 설정 (AWS SES 권장)
EMAIL_BACKEND=django_ses.SESBackend
AWS_ACCESS_KEY_ID=본인-AWS-키
AWS_SECRET_ACCESS_KEY=본인-AWS-시크릿
AWS_SES_REGION_NAME=us-east-1
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# 🤖 AI 기능 (선택사항)
ANTHROPIC_API_KEY=본인-앤트로픽-API-키

# 🗄️ 외부 데이터베이스 사용시 (RDS 사용시)
# DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/resee_production

# 🔄 외부 캐시 사용시 (ElastiCache 사용시)
# REDIS_URL=redis://your-redis-cluster-endpoint:6379/1
# CELERY_RESULT_BACKEND=redis://your-redis-cluster-endpoint:6379/0
```

### 🔑 보안 키 생성 도구
```bash
# Django SECRET_KEY 생성
python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# 강한 비밀번호 생성
openssl rand -base64 32
```

---

## 🔗 4단계: AWS ALB 설정 (15분)

### Application Load Balancer 생성
1. **ALB 생성**: EC2 → Load Balancers → Create Load Balancer
2. **타겟 그룹**: EC2 인스턴스를 타겟으로 설정 (포트 80)
3. **SSL 인증서**: AWS Certificate Manager에서 발급 후 HTTPS 리스너에 연결
4. **헬스체크**: `/api/health/` 경로로 설정

### Route 53 DNS 설정 (선택사항)
```bash
# 도메인을 ALB로 연결
yourdomain.com → ALB DNS 이름 (A record 또는 ALIAS)
www.yourdomain.com → ALB DNS 이름 (A record 또는 ALIAS)
```

---

## 🚢 5단계: 애플리케이션 배포 (20분)

### Docker 이미지 빌드 및 실행
```bash
cd /opt/Resee

# 1. 이미지 빌드 (5분)
docker-compose -f docker-compose.production.yml build

# 2. 데이터베이스 서비스만 먼저 시작 (2분)
docker-compose -f docker-compose.production.yml up -d db redis rabbitmq

# 3. 30초 대기 후 데이터베이스 초기화 (3분)
sleep 30
docker-compose -f docker-compose.production.yml run --rm backend python manage.py migrate
docker-compose -f docker-compose.production.yml run --rm backend python manage.py collectstatic --noinput

# 4. 관리자 계정 생성 (2분)
docker-compose -f docker-compose.production.yml run --rm backend python manage.py createsuperuser

# 5. 전체 서비스 시작 (3분)
docker-compose -f docker-compose.production.yml up -d

# 6. 서비스 상태 확인
docker-compose -f docker-compose.production.yml ps
```

### 시스템 서비스로 등록 (자동 재시작)
```bash
# Docker Compose 서비스 등록
sudo tee /etc/systemd/system/resee.service << EOF
[Unit]
Description=Resee Application
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/Resee
ExecStart=/usr/local/bin/docker-compose -f docker-compose.production.yml up -d
ExecStop=/usr/local/bin/docker-compose -f docker-compose.production.yml down
TimeoutStartSec=0
User=ubuntu

[Install]
WantedBy=multi-user.target
EOF

# 서비스 활성화
sudo systemctl daemon-reload
sudo systemctl enable resee.service
sudo systemctl start resee.service
```

---

## ✅ 6단계: 배포 완료 확인 (10분)

### ALB 헬스체크 확인
```bash
# EC2 인스턴스에서 직접 확인
curl http://localhost/api/health/

# ALB를 통해 확인
curl https://yourdomain.com/api/health/
```

### 브라우저에서 확인할 것들
- [ ] **메인 페이지**: https://yourdomain.com 접속 확인
- [ ] **HTTPS 리다이렉션**: HTTP → HTTPS 자동 전환 확인
- [ ] **회원가입**: 이메일 회원가입 테스트 
- [ ] **Google 로그인**: Google OAuth 로그인 테스트
- [ ] **콘텐츠 생성**: 학습 콘텐츠 생성 테스트
- [ ] **복습 기능**: 복습 페이지 동작 확인
- [ ] **관리자 페이지**: https://yourdomain.com/admin/ 접속 확인

---

## 🔧 자동 백업 설정 (선택사항)

### 데이터베이스 자동 백업
```bash
# 백업 스크립트 실행 권한 부여
chmod +x scripts/backup_db.sh

# 매일 새벽 2시 자동 백업 설정
crontab -e
# 다음 줄 추가:
0 2 * * * /opt/Resee/scripts/backup_db.sh >> /var/log/resee/backup.log 2>&1
```

---

## 🚨 AWS 환경 문제 해결

### ALB 헬스체크 실패
```bash
# 헬스체크 엔드포인트 확인
curl -I http://localhost/api/health/

# nginx 컨테이너 로그 확인
docker-compose -f docker-compose.production.yml logs nginx

# 백엔드 상태 확인
docker-compose -f docker-compose.production.yml logs backend
```

### SSL 인증서 문제 (AWS Certificate Manager)
- ACM에서 인증서 상태 확인
- 도메인 검증 완료 여부 확인
- ALB 리스너에 인증서 연결 확인

### 환경변수 문제
```bash
# 환경변수 파일 확인 (민감정보 제외)
grep -v -E "(PASSWORD|SECRET|KEY)" .env.production

# 컨테이너 환경변수 확인
docker-compose -f docker-compose.production.yml exec backend env | grep -v -E "(PASSWORD|SECRET|KEY)"
```

### 정적파일 문제 (CSS/JS 안 로드)
```bash
# 정적파일 재수집
docker-compose -f docker-compose.production.yml exec backend python manage.py collectstatic --noinput

# nginx 설정 확인
docker-compose -f docker-compose.production.yml exec nginx nginx -t
```

### RDS 연결 문제 (외부 DB 사용시)
```bash
# VPC 보안 그룹 확인 (5432 포트 열려있는지)
# RDS 엔드포인트 연결 테스트
telnet your-rds-endpoint 5432

# Django에서 DB 연결 테스트
docker-compose -f docker-compose.production.yml exec backend python manage.py dbshell
```

---

## 🎉 AWS 배포 완료!

### ✅ 성공하면 이런 것들이 동작해야 합니다:
- https://yourdomain.com → 메인 페이지 로드 (ALB를 통해)
- HTTP → HTTPS 자동 리다이렉션 (ALB에서 처리)
- 회원가입/로그인 기능
- Google OAuth 로그인
- 콘텐츠 생성 및 복습 기능
- 관리자 페이지 접속
- SSL 인증서 자동 갱신 (AWS에서 처리)

### 🔄 AWS 환경 장점
- **자동 스케일링**: ALB + Auto Scaling Group으로 트래픽 대응
- **고가용성**: Multi-AZ 배포로 장애 대응
- **관리형 서비스**: RDS, ElastiCache로 인프라 관리 부담 감소
- **보안**: AWS IAM, Security Groups으로 세밀한 접근 제어
- **모니터링**: CloudWatch로 실시간 모니터링

### 📊 정기 관리
- **일일**: CloudWatch 메트릭 확인
- **주간**: `ls -la /backups/postgresql/` 백업 확인
- **월간**: EC2 인스턴스 보안 업데이트

---

## 🚀 고급 AWS 설정 (선택사항)

### CloudFront CDN 추가
```bash
# 정적 파일 가속화를 위한 CloudFront 설정
# ALB를 오리진으로 하는 CloudFront 배포 생성
```

### Auto Scaling 설정
```bash
# EC2 인스턴스를 AMI로 만들어 Auto Scaling Group 구성
# 트래픽에 따른 자동 스케일링 설정
```

### 모니터링 강화
```bash
# CloudWatch 알람 설정
# - CPU 사용률 80% 이상
# - 메모리 사용률 80% 이상  
# - ALB 응답 시간 5초 이상
# - 5xx 에러 발생 시
```

---

## 📚 취업 포트폴리오용 AWS 아키텍처 선택

### 🏗️ 아키텍처 1: 기본 구성 ($20-30/월)
**적합한 경우**: 신입 개발자, 실무 가능성 증명

```
[Route53]
    ↓
[ALB + ACM 인증서]
    ↓
[EC2 단일 인스턴스]
    - Docker Compose 전체 스택
    - Auto Scaling Ready
    ↓
[CloudFront] → 정적 자산
[RDS] → PostgreSQL (프리티어)
```

**면접 포인트**:
- Docker 기반 마이크로서비스 이해
- ALB를 통한 SSL/TLS 처리
- CloudFront CDN 활용
- RDS 관리형 데이터베이스 경험

### 🚀 아키텍처 2: 고급 구성 ($50-70/월)
**적합한 경우**: 경력 개발자, 대규모 시스템 경험 증명

```
[Route53]
    ↓
[ALB + WAF]
    ↓
[ECS Fargate 클러스터]
    - Backend 서비스
    - Frontend 서비스
    - Celery Worker
    ↓
[RDS Multi-AZ] → 고가용성 DB
[ElastiCache] → Redis 클러스터
[EFS] → 공유 스토리지
[CloudWatch] → 모니터링
```

**면접 포인트**:
- 컨테이너 오케스트레이션 (ECS)
- 무중단 배포 전략
- Multi-AZ 고가용성
- 분산 캐싱 전략
- 시스템 모니터링 및 알람

### ⚡ 아키텍처 3: 서버리스 ($10-20/월)
**적합한 경우**: 클라우드 네이티브 역량 증명

```
[CloudFront + S3] → React 정적 호스팅
    ↓
[API Gateway]
    ↓
[Lambda Functions]
    - Django Lambda Adapter
    - 비동기 작업 처리
    ↓
[DynamoDB] → NoSQL
[SQS] → 메시지 큐
[EventBridge] → 스케줄러
```

**면접 포인트**:
- 서버리스 아키텍처 설계
- 이벤트 기반 아키텍처
- NoSQL 데이터 모델링
- 비용 최적화 (사용한 만큼만 과금)
- 자동 스케일링

---

## 🎯 취업 면접을 위한 추천 구성

### 하이브리드 아키텍처 (추천) ($30-40/월)
**컨테이너 + 서버리스 조합**으로 실무 역량 종합 증명

```yaml
# 인프라 구성
Frontend: S3 + CloudFront (서버리스)
Backend API: ECS Fargate (컨테이너)
Background Jobs: Lambda + SQS (서버리스)
Database: RDS PostgreSQL (관리형)
Cache: ElastiCache Redis (관리형)
File Storage: S3 (서버리스)
Monitoring: CloudWatch + X-Ray
CI/CD: GitHub Actions + ECR + CodeDeploy
```

### 구현 로드맵
```bash
# 1단계: 기본 인프라 (1주차)
- VPC, Subnet, Security Group 설정
- RDS, ElastiCache 프로비저닝
- S3 버킷 생성 (정적 파일, 백업)

# 2단계: 컨테이너화 (2주차)
- ECR 레지스트리 생성
- Docker 이미지 최적화
- ECS 클러스터 및 서비스 정의

# 3단계: 서버리스 통합 (3주차)
- Lambda 함수 작성 (이미지 처리, 알림 등)
- API Gateway 설정
- EventBridge 스케줄러 구성

# 4단계: 운영 최적화 (4주차)
- CloudWatch 대시보드 구성
- 비용 알람 설정
- 백업 자동화
- 보안 점검 (Security Hub)
```

---

## 💼 면접 대비 체크리스트

### 기술적 질문 대비
```markdown
✅ "왜 이 아키텍처를 선택했나요?"
   → 비용, 확장성, 운영 편의성 트레이드오프 설명

✅ "트래픽이 10배 증가하면?"
   → Auto Scaling, Read Replica, 캐싱 전략 설명

✅ "장애 대응 시나리오는?"
   → Multi-AZ, 헬스체크, Circuit Breaker 패턴 설명

✅ "보안은 어떻게 구현했나요?"
   → IAM 역할, Security Group, WAF, Secrets Manager 활용

✅ "비용 최적화 방법은?"
   → Reserved Instance, Spot Instance, S3 라이프사이클 정책

✅ "모니터링 전략은?"
   → 4 Golden Signals (지연시간, 트래픽, 에러, 포화도)
   → CloudWatch 커스텀 메트릭, X-Ray 분산 추적

✅ "CI/CD 파이프라인은?"
   → GitHub Actions → ECR → ECS Blue/Green 배포
```

### 실무 경험 어필 포인트
```yaml
DevOps 역량:
  - Infrastructure as Code (Terraform/CloudFormation)
  - 컨테이너 오케스트레이션 (ECS/EKS)
  - 모니터링 및 로깅 (CloudWatch/ELK)
  - CI/CD 파이프라인 구축

클라우드 네이티브:
  - 12 Factor App 원칙 준수
  - 마이크로서비스 아키텍처
  - 이벤트 기반 아키텍처
  - 서버리스 컴퓨팅

보안 및 컴플라이언스:
  - 최소 권한 원칙 (IAM)
  - 저장 및 전송 중 암호화
  - 보안 그룹 및 NACL 설정
  - AWS Well-Architected Framework 준수
```

---

## 📊 비용 최적화 전략

### 월 예산별 구성
```bash
# $10-20/월 (MVP)
- EC2 t3.micro (프리티어)
- RDS t3.micro (프리티어)
- S3 + CloudFront (사용량 기반)

# $30-50/월 (프로덕션 준비)
- EC2 t3.small/medium
- RDS t3.small + 자동 백업
- ElastiCache t3.micro
- ALB + Route53

# $100+/월 (실제 서비스)
- ECS Fargate 또는 EKS
- RDS Multi-AZ
- ElastiCache 클러스터
- CloudWatch 상세 모니터링
- AWS WAF + Shield
```

### 비용 절감 팁
```yaml
개발/테스트 환경:
  - 업무 시간외 자동 중지 (Lambda + EventBridge)
  - Spot Instance 활용 (70% 절감)
  - 개발용 RDS는 단일 AZ

프로덕션 환경:
  - Reserved Instance (1년 약정 시 40% 절감)
  - S3 Intelligent-Tiering
  - CloudFront 캐싱 최적화
  - 불필요한 EBS 스냅샷 정리

모니터링:
  - AWS Cost Explorer 일일 체크
  - Budget Alert 설정 ($50, $100)
  - Trusted Advisor 권장사항 적용
```

---

## 🔗 추가 리소스

### AWS 학습 자료
- [AWS Well-Architected Labs](https://wellarchitectedlabs.com/)
- [AWS Architecture Center](https://aws.amazon.com/architecture/)
- [AWS 한국 블로그](https://aws.amazon.com/ko/blogs/korea/)

### 인증 취득 추천
1. **AWS Cloud Practitioner** (기초)
2. **AWS Solutions Architect Associate** (필수)
3. **AWS Developer Associate** (선택)

### 포트폴리오 작성 팁
```markdown
1. 아키텍처 다이어그램 필수 (draw.io, Lucidchart)
2. 비용 분석 포함 (월별 예상 비용)
3. 성능 메트릭 제시 (응답시간, 처리량)
4. 보안 체크리스트 문서화
5. 트러블슈팅 경험 기록
6. GitHub README에 배포 링크 포함
```

---

**🚀 이제 Resee가 AWS 프로덕션에서 안전하고 확장 가능하게 실행됩니다!**

> 💡 **취업 성공 팁**: 단순 배포가 아닌, 비용/성능/보안을 고려한 아키텍처 설계 역량을 어필하세요!