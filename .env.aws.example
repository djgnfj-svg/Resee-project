# AWS SES 이메일 발송 설정

# 이메일 백엔드 설정
EMAIL_BACKEND=django_ses.SESBackend

# AWS 자격증명 (IAM 사용자)
AWS_ACCESS_KEY_ID=AKIA...your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS SES 지역 설정 (서울 리전)
AWS_SES_REGION_NAME=ap-northeast-2
AWS_SES_REGION_ENDPOINT=email.ap-northeast-2.amazonaws.com

# 발신자 이메일 (SES에서 인증된 이메일 또는 도메인)
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# 프론트엔드 URL
FRONTEND_URL=http://localhost

# 기타 이메일 설정
EMAIL_VERIFICATION_TIMEOUT_DAYS=7
EMAIL_VERIFICATION_RESEND_MINUTES=5

# === AWS SES 설정 가이드 ===

# 1. AWS 계정 생성 및 IAM 사용자 생성
#    - IAM에서 새 사용자 생성
#    - 정책: AmazonSESFullAccess 또는 세부 권한 설정

# 2. SES 이메일/도메인 인증
#    - 개발용: 이메일 주소 인증 (noreply@yourdomain.com)
#    - 프로덕션: 도메인 전체 인증 권장

# 3. 샌드박스 모드 해제 (프로덕션용)
#    - 기본적으로 SES는 샌드박스 모드 (인증된 이메일만 발송)
#    - 프로덕션에서는 샌드박스 해제 신청 필요

# 4. 테스트 방법
#    - 먼저 인증된 이메일로 테스트
#    - 성공 시 샌드박스 해제 신청