# 추가 구현 아이디어 - 2025년 백엔드 신입 취업 강화

> **작성일**: 2025-10-21
> **목적**: 포트폴리오 차별화를 위한 추가 구현 후보

---

## 🔥 2025년 백엔드 신입 취업 최강 주제 TOP 5

### 1위: **Redis 캐싱 전략 (Cache-aside Pattern)** ⭐⭐⭐⭐⭐

**추천 이유**: 즉시 구현 가능 + 극적인 성과 + 면접 단골 질문

#### 구현 내용

**캐싱 대상 API**:
```python
1. GET /api/review/today/ (오늘의 복습 목록)
   - Before: 200ms (DB 조회)
   - After: 10ms (Redis 캐싱, TTL: 1시간)
   - 성과: 95% 단축

2. GET /api/analytics/stats/ (학습 통계)
   - Before: 500ms (복잡한 집계 쿼리)
   - After: 20ms (Redis 캐싱, TTL: 5분)
   - 성과: 96% 단축

3. GET /api/content/?page=1 (콘텐츠 목록)
   - Before: 150ms
   - After: 15ms (페이지별 캐싱, TTL: 10분)
   - 성과: 90% 단축
```

**구현 코드 예시**:
```python
# review/views.py
from django.core.cache import cache

class TodayReviewListView(APIView):
    def get(self, request):
        cache_key = f'review:today:{request.user.id}'

        # 캐시 확인
        cached_data = cache.get(cache_key)
        if cached_data:
            return Response(cached_data)

        # DB 조회
        reviews = ReviewSchedule.objects.filter(
            user=request.user,
            next_review_date__lte=timezone.now().date(),
            is_active=True
        ).select_related('content')

        serializer = ReviewScheduleSerializer(reviews, many=True)

        # 캐시 저장 (TTL: 1시간)
        cache.set(cache_key, serializer.data, timeout=3600)

        return Response(serializer.data)
```

**캐시 무효화 전략**:
```python
# review/views.py
class CompleteReviewView(APIView):
    def post(self, request, schedule_id):
        # 복습 제출
        # ...

        # 캐시 무효화
        cache_keys = [
            f'review:today:{request.user.id}',
            f'analytics:stats:{request.user.id}',
        ]
        cache.delete_many(cache_keys)

        return Response(result)
```

#### 측정 가능한 성과

- 평균 API 응답: 250ms → 50ms (**80% 단축**)
- DB 부하: 1000 queries/min → 200 queries/min (**80% 감소**)
- Redis hit rate: **85%+**

#### 면접 강점

- "캐싱 전략 어떻게 세우나요?"
  * Cache-aside pattern 설명
  * TTL 설정 기준 (데이터 변경 빈도)

- "Cache invalidation 어떻게 하나요?"
  * Write-through: 데이터 변경 시 즉시 캐시 삭제
  * 사용자별 캐시 키 설계

- "Cache stampede 어떻게 방지하나요?"
  * Lock을 사용한 캐시 재생성
  * Probabilistic early expiration

#### 구현 난이도
⭐⭐ (쉬움, Redis 이미 설치됨)

#### 구현 시간
1-2일

---

### 2위: **Prometheus + Grafana 모니터링** ⭐⭐⭐⭐⭐

**추천 이유**: 차별화 최강 + 시니어도 부족한 영역

#### 구현 내용

**Docker Compose 설정**:
```yaml
# docker-compose.yml
prometheus:
  image: prom/prometheus:latest
  ports:
    - "9090:9090"
  volumes:
    - ./prometheus.yml:/etc/prometheus/prometheus.yml
    - prometheus_data:/prometheus
  command:
    - '--config.file=/etc/prometheus/prometheus.yml'

grafana:
  image: grafana/grafana:latest
  ports:
    - "3001:3000"
  volumes:
    - grafana_data:/var/lib/grafana
  environment:
    - GF_SECURITY_ADMIN_PASSWORD=admin
  depends_on:
    - prometheus
```

**Prometheus 설정**:
```yaml
# prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'django'
    static_configs:
      - targets: ['backend:8000']

  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres_exporter:9187']

  - job_name: 'redis'
    static_configs:
      - targets: ['redis_exporter:9121']
```

**Django 메트릭 수집**:
```python
# requirements.txt
django-prometheus==2.3.1

# settings.py
INSTALLED_APPS = [
    'django_prometheus',
    # ...
]

MIDDLEWARE = [
    'django_prometheus.middleware.PrometheusBeforeMiddleware',
    # ...
    'django_prometheus.middleware.PrometheusAfterMiddleware',
]

# urls.py
urlpatterns += [
    path('metrics/', include('django_prometheus.urls')),
]
```

#### 측정 가능한 성과

- 장애 감지 시간: 30분 → 1분 (**97% 단축**)
- 실시간 메트릭 대시보드:
  * API 응답 시간 (P50, P95, P99)
  * CPU/메모리 사용률
  * DB 연결 풀 사용량
  * Redis hit/miss rate
  * Celery task 실행 시간
- Alert 자동 발송: Slack 연동

#### Grafana 대시보드 구성

1. **API Performance**
   - 요청 수 (RPS)
   - 응답 시간 분포
   - 에러율

2. **Infrastructure**
   - CPU/메모리 사용률
   - Disk I/O
   - Network throughput

3. **Database**
   - 연결 풀 사용량
   - Slow query count
   - Transaction rate

4. **Redis**
   - Hit/Miss rate
   - Memory usage
   - Eviction count

#### 면접 강점

- "프로덕션 모니터링 어떻게 하나요?"
  * Prometheus + Grafana 스택 설명
  * 메트릭 수집 방법

- "장애 어떻게 감지하나요?"
  * Alert 규칙 설정
  * Slack 알림 연동

- "메트릭 수집 어떻게 하나요?"
  * django-prometheus 미들웨어
  * Exporter 패턴

#### 구현 난이도
⭐⭐⭐⭐ (복잡, 하지만 강력)

#### 구현 시간
1주일

---

### 3위: **Database Connection Pool 최적화** ⭐⭐⭐⭐

**추천 이유**: 실무 필수 지식 + 쉽게 구현 가능

#### 구현 내용

**Django 설정**:
```python
# settings/base.py
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'resee_prod',
        'USER': 'postgres',
        'PASSWORD': 'password',
        'HOST': 'postgres',
        'PORT': 5432,

        # Connection Pool 설정
        'CONN_MAX_AGE': 600,  # 연결 재사용 (10분)
        'CONN_HEALTH_CHECKS': True,  # 연결 상태 체크

        'OPTIONS': {
            'connect_timeout': 10,  # 연결 타임아웃 10초
            'options': '-c statement_timeout=30000',  # SQL 타임아웃 30초
        },
    }
}
```

**PostgreSQL 설정**:
```sql
-- postgresql.conf
max_connections = 100
shared_buffers = 256MB
effective_cache_size = 1GB
work_mem = 4MB
maintenance_work_mem = 64MB
```

**Connection Pool 계산 공식**:
```
max_connections = (gunicorn_workers × threads_per_worker) + celery_workers + 여유(20%)

예시 (Resee):
= (2 × 2) + 1 + 2
= 7개 필요
→ PostgreSQL max_connections: 100 (충분)
```

#### 트러블슈팅 스토리

**문제 상황**:
```
FATAL: remaining connection slots are reserved for non-replication superuser connections
```

**원인 분석**:
- Gunicorn workers: 2개
- Threads per worker: 2개
- Celery workers: 1개
- 총 필요 연결: 5개
- PostgreSQL max_connections: 기본값 100개
- → 문제 없어야 하는데 왜?

**실제 원인**:
- `CONN_MAX_AGE=0` (연결 재사용 안 함)
- 매 요청마다 새 연결 생성/종료
- 동시 요청 많을 때 연결 부족

**해결 방법**:
```python
CONN_MAX_AGE = 600  # 10분 재사용
```

#### 측정 가능한 성과

- Connection timeout: 발생 빈도 **100% → 0%**
- 동시 접속 처리: 50명 → 100명 (**2배**)
- Connection 생성 오버헤드: 10ms → 0ms (재사용)
- DB 부하: 연결 생성/종료 부하 **제거**

#### 면접 강점

- "Connection pool 어떻게 설정하나요?"
  * `CONN_MAX_AGE` 설정
  * 계산 공식

- "max_connections 계산 공식은?"
  * workers × threads + celery + 여유

- "Connection timeout 어떻게 처리하나요?"
  * `connect_timeout` 설정
  * Health check 활성화

#### 구현 난이도
⭐⭐ (쉬움, 설정만 변경)

#### 구현 시간
반나절 (문서화 포함)

---

### 4위: **APM (django-silk or Sentry)** ⭐⭐⭐⭐

**추천 이유**: 성능 병목 시각화 + 실무 필수

#### 구현 내용 (django-silk)

**설치**:
```bash
pip install django-silk==5.0.4
```

**설정**:
```python
# settings.py
INSTALLED_APPS = [
    'silk',
    # ...
]

MIDDLEWARE = [
    'silk.middleware.SilkyMiddleware',  # 최상단
    # ...
]

# Silk 설정
SILKY_PYTHON_PROFILER = True
SILKY_PYTHON_PROFILER_BINARY = True
SILKY_MAX_REQUEST_BODY_SIZE = 1024  # 1KB
SILKY_MAX_RESPONSE_BODY_SIZE = 1024

# urls.py
urlpatterns += [
    path('silk/', include('silk.urls', namespace='silk'))
]
```

**사용 예시**:
```python
from silk.profiling.profiler import silk_profile

@silk_profile(name='Calculate Review Stats')
def calculate_review_stats(user):
    # 복잡한 통계 계산
    return stats
```

#### 측정 가능한 성과

- Slow query 자동 감지: 100ms 이상 쿼리 자동 기록
- API 응답 시간 분석: 어느 API가 느린지 한눈에 파악
- SQL 쿼리 프로파일링: N+1 자동 감지
- Python 코드 프로파일링: 함수별 실행 시간

#### Silk 대시보드 기능

1. **Requests**: 모든 API 요청 목록
   - URL, Method, 응답 시간
   - Query count, Time spent in DB

2. **SQL Queries**: 실행된 모든 SQL
   - Query text
   - Execution time
   - Stack trace

3. **Profiling**: Python 함수 실행 시간
   - 함수별 실행 시간
   - Call stack

4. **Summary**: 전체 통계
   - 가장 느린 API TOP 10
   - 가장 많이 실행된 Query TOP 10

#### 면접 강점

- "성능 병목 어떻게 찾나요?"
  * django-silk로 프로파일링
  * Slow query 자동 감지

- "N+1 쿼리 어떻게 발견했나요?"
  * Silk에서 Query count 확인
  * 동일 쿼리 반복 패턴 감지

#### 구현 난이도
⭐⭐⭐ (중간)

#### 구현 시간
1-2일

---

### 5위: **Database Read Replica (Master-Slave)** ⭐⭐⭐⭐⭐

**추천 이유**: 시니어 수준 + Scale-out 경험

#### 구현 내용

**Django Database Router**:
```python
# settings.py
DATABASES = {
    'default': {  # Master (Write)
        'ENGINE': 'django.db.backends.postgresql',
        'HOST': 'db-master.reseeall.com',
        'NAME': 'resee_prod',
    },
    'replica': {  # Slave (Read)
        'ENGINE': 'django.db.backends.postgresql',
        'HOST': 'db-replica.reseeall.com',
        'NAME': 'resee_prod',
    }
}

DATABASE_ROUTERS = ['routers.ReplicaRouter']
```

**Database Router 구현**:
```python
# routers.py
class ReplicaRouter:
    def db_for_read(self, model, **hints):
        """읽기는 Replica로"""
        return 'replica'

    def db_for_write(self, model, **hints):
        """쓰기는 Master로"""
        return 'default'

    def allow_relation(self, obj1, obj2, **hints):
        """Master-Replica 간 관계 허용"""
        return True

    def allow_migrate(self, db, app_label, model_name=None, **hints):
        """Migration은 Master만"""
        return db == 'default'
```

**Replication Lag 처리**:
```python
# 쓰기 직후 읽기 → Master 사용
from django.db import transaction

class ContentCreateView(APIView):
    def post(self, request):
        with transaction.atomic():
            content = Content.objects.create(...)

            # 쓰기 직후 읽기 → Master 강제 사용
            content_detail = Content.objects.using('default').get(id=content.id)

        return Response(ContentSerializer(content_detail).data)
```

#### AWS RDS 설정

**Master 인스턴스**:
- db.t3.small
- Multi-AZ: Yes (HA)

**Read Replica**:
- db.t3.small
- 동일 Region (지연 최소화)

**비용**:
- Master: $30/month
- Replica: $30/month
- 총: $60/month

#### 측정 가능한 성과

- 읽기 성능: **2배 향상** (Master 부하 분산)
- Master DB 부하: 100% → 50%
- 동시 처리: 100 TPS → 200 TPS
- 읽기 쿼리 응답 시간: 50ms → 25ms

#### 트러블슈팅: Replication Lag

**문제**:
- User가 Content 생성 → 바로 목록 조회
- Replica에 아직 복제 안 됨 → 새 Content 안 보임

**해결**:
```python
# 쓰기 직후 읽기는 Master 사용
Content.objects.using('default').filter(author=request.user)
```

#### 면접 강점

- "Database Scale-out 어떻게 하나요?"
  * Master-Slave 복제 구조
  * Read Replica 활용

- "Master-Slave 복제 지연 어떻게 처리하나요?"
  * Replication lag 모니터링
  * 쓰기 직후 읽기는 Master 사용

- "Write 후 Read 일관성 어떻게 보장하나요?"
  * `using('default')` 명시
  * Sticky session

#### 구현 난이도
⭐⭐⭐⭐⭐ (매우 복잡, AWS RDS 필요)

#### 구현 시간
1-2주 (AWS 설정 포함)

---

## 🎯 구현 우선순위 (추천)

### **즉시 구현 (1주일 이내)**:
1. **Redis 캐싱** ⭐⭐⭐⭐⭐ **(가장 추천!)**
   - 시간: 1-2일
   - 성과: API 응답 80% 단축
   - 난이도: 쉬움

2. **Connection Pool 최적화** ⭐⭐⭐⭐
   - 시간: 반나절
   - 성과: Connection timeout 제거
   - 난이도: 매우 쉬움

### **도전 가치 있음 (2-3주)**:
3. **django-silk APM** ⭐⭐⭐⭐
   - 시간: 1-2일
   - 성과: 성능 병목 시각화
   - 난이도: 중간

4. **Prometheus + Grafana** ⭐⭐⭐⭐⭐
   - 시간: 1주일
   - 성과: 실시간 모니터링
   - 난이도: 복잡 (하지만 차별화 최강!)

### **장기 프로젝트 (1-2개월)**:
5. **Database Read Replica** ⭐⭐⭐⭐⭐
   - 시간: 1-2주
   - 성과: Scale-out 경험
   - 난이도: 매우 복잡

---

## 💡 개인적 추천

**단기 (이번 주)**:
1. Redis 캐싱 구현 (1-2일)
2. Connection Pool 문서화 (반나절)

**중기 (이번 달)**:
3. django-silk 설치 + 프로파일링 (1-2일)
4. Prometheus + Grafana 구축 (1주일)

**장기 (여유 있을 때)**:
5. Read Replica 구축 (AWS RDS 필요)

---

## 🚫 제외된 아이디어

### **LangChain으로 주간 시험 성능 향상**
**제외 이유**:
- ❌ 측정 가능한 성과 불명확 ("성능 향상"이 모호)
- ❌ 이미 Anthropic SDK 직접 사용 중 (claude-3-haiku)
- ❌ LangChain 추가 = 불필요한 복잡도 증가
- ❌ 면접에서 "왜 LangChain?" 질문에 답변 어려움

**현재 방식이 더 나은 이유**:
```python
# 현재: Anthropic SDK 직접 사용
from anthropic import Anthropic
client = Anthropic(api_key=settings.ANTHROPIC_API_KEY)
response = client.messages.create(...)

# LangChain 사용 시
from langchain_anthropic import ChatAnthropic
from langchain_core.prompts import ChatPromptTemplate
# ... 불필요한 boilerplate 코드 증가
```

**LangChain이 유용한 경우**:
- RAG (Retrieval-Augmented Generation) 구현
- 복잡한 Chain 구성 (Multi-step reasoning)
- Agent 시스템 (Tool calling)

**Resee는 단순 프롬프트만 사용** → LangChain 불필요

---

**작성일**: 2025-10-21
**최종 업데이트**: 2025-10-21
